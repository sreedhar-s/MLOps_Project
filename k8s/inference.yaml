apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: churn-predictor
  namespace: churn-model
spec:
  predictor:
    serviceAccountName: s3-sa
    sklearn:
      storageUri: s3://mlops-s3-bucket1/models/